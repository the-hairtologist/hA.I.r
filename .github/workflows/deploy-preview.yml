# WORKFLOW DISABLED - Using Lovable's Built-in Preview System
#
# This workflow has been disabled because this project uses Lovable's
# built-in preview system instead of custom GitHub Actions deployments.
#
# Lovable automatically creates preview environments for each branch.
# No additional configuration needed.

name: Deploy Preview (Disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    name: Workflow Disabled
    runs-on: ubuntu-latest
    steps:
      - name: Disabled Notice
        run: echo "This workflow is disabled. Using Lovable's built-in preview system."
